package com.company.structural.bridge.tiendas;

/**
 * "Nueva" interface que se crea con la API que queremos sea visible a los nuevos clientes.
 */
public interface VendedorOnline {

	String descripcionProducto (String producto);
}
